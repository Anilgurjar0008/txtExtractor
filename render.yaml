# render.yaml – Free-plan Telegram Bot
services:
  - type: web
    name: txt-extractor-bot
    env: python
    region: singapore        # Free tier available in oregon
    plan: free            # 👈 FREE tier
    buildCommand: pip install -r requirements.txt
    startCommand: python -m bot
    envVars:
      - key: BOT_TOKEN    # @BotFather se
        sync: false
      - key: API_ID       # my.telegram.org se
        sync: false
      - key: API_HASH     # my.telegram.org se
        sync: false
